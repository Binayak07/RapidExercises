const rows=Number(prompt("Enter number of rows: "));
const columns=Number(prompt("Enter number of cols: "));
for(let r=0;r<rows;r++)
{
	for(let c=0;c<cols;c++)
	{
		console.log("Row "+r+" Col "+c);
	}
	console.log("");
}